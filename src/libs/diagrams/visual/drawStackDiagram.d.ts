import type { StackDiagram } from './types.js';
import type { SVG } from '../../diagram-api/types.js';
export declare const drawStackDiagram: (svg: SVG, stackDiagram: StackDiagram, yOffset: number, component_id: number) => void;
